export enum TransactionType {
    INCOME = "income",
    EXPENSE = "expense",
    TRANSFER = "transfer"
}

export enum TransactionCategory {
    BANKING = "banking",
    STOCKS = "stocks",
    CRYPTOS = "cryptos"
}